target/

# Environment configuration
.env

.claude/skills/linux/linux/
freebsd-src/
*.pdf
*.log

__pycache__/

# Event-stream / determinism run output
*.jsonl

# Nix
result
result-*

# Workload container archives: built transiently by `just build-workload`
# (staged with `git add -f` only for the duration of a nix build).
workloads/*/images.tar

# Kernel module build artifacts
crates/bedrock/*.o
crates/bedrock/*.ko
crates/bedrock/*.mod
crates/bedrock/*.mod.c
crates/bedrock/*.cmd
crates/bedrock/.*.cmd
crates/bedrock/Module.symvers
crates/bedrock/modules.order
